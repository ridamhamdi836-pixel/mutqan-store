/**
 * Session replay / heatmaps (Hotjar) — public site ID (same as in page HTML).
 * Easypanel HOTJAR_SITE_ID still overrides when set.
 */
export const WEB_ANALYTICS = {
  /** Numeric Site ID from Hotjar → Sites & Organizations → Tracking code */
  hotjarSiteId: "6725614",
} as const;
